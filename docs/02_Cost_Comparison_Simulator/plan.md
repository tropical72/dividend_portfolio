# Plan: Personal vs Corporate Cost Comparison Simulator

## 🔄 진행 현황 (Progress Mapping)

### [Phase 1] 요구사항 및 도메인 경계 확정 (T-CCS-1) - DONE
- **T-CCS-1.1: 도메인 목표 명확화**
    - [x] **T-CCS-1.1.1** 본 기능의 목적을 `개인운용 100% vs 법인운용 100%` 비교로 고정한다.
    - [x] **T-CCS-1.1.2** 비교의 본질을 `투자전략 비교`가 아닌 `운용 구조 비교`로 정의한다.
- **T-CCS-1.2: 핵심 전제 확정**
    - [x] **T-CCS-1.2.1** 기준 포트폴리오는 `활성 master portfolio`를 사용한다.
    - [x] **T-CCS-1.2.2** 두 시나리오에 동일한 `DY + PA = TR`을 적용한다.
    - [x] **T-CCS-1.2.3** 부동산은 건보료용 재산으로만 반영한다.
    - [x] **T-CCS-1.2.4** 개인연금은 비교 포트폴리오 밖의 별도 고정자산으로 둔다.
    - [x] **T-CCS-1.2.5** 결과 KPI는 `월 실수령액`, `연 총비용`, `건보료`, `세후 순증가액`으로 고정한다.
- **T-CCS-1.3: 문서 초안 작성**
    - [x] **T-CCS-1.3.1** 요구사항 문서 초안 작성 완료.

### [Phase 2] 백엔드 데이터 모델 및 저장 스키마 설계 (T-CCS-2) - DONE
- **T-CCS-2.1: 비교 전용 설정 스키마 정의**
    - [x] **T-CCS-2.1.1** `cost_comparison_config.json` 또는 동등한 저장 구조를 설계한다.
    - [x] **T-CCS-2.1.2** 기존 `settings.json` 및 `retirement_config.json`과 충돌하지 않는 전용 네임스페이스를 정의한다.
    - [x] **T-CCS-2.1.3** 하위 호환을 위한 기본값 마이그레이션 규칙을 정의한다.
- **T-CCS-2.2: 입력 모델 확정**
    - [x] **T-CCS-2.2.1** 가구 구성원 스키마(`relation`, `birth_year_month/age`, `is_dependent_candidate`)를 정의한다.
    - [x] **T-CCS-2.2.2** 부동산 입력 스키마(`official_price`, `ownership_ratio`)를 정의한다.
    - [x] **T-CCS-2.2.3** 법인 급여 수령자 스키마(`name`, `relationship`, `monthly_salary`, `is_employee_insured`)를 정의한다.
    - [x] **T-CCS-2.2.4** 주주대여금 입력 스키마(`initial_balance`, `annual_repayment_target`)를 정의한다.
    - [x] **T-CCS-2.2.5** 시뮬레이션 기간 및 PA 입력 스키마를 정의한다.
- **T-CCS-2.3: API 계약 초안**
    - [x] **T-CCS-2.3.1** 설정 조회/저장 API request/response 모델을 정의한다.
    - [x] **T-CCS-2.3.2** 비교 실행 API response 구조(KPI, breakdown, charts, assumptions)를 정의한다.

### [Phase 3] 백엔드 비교 엔진 구현 (T-CCS-3) - DONE
- **T-CCS-3.1: 기준 포트폴리오/수익률 주입 계층**
    - [x] **T-CCS-3.1.1** 활성 master portfolio에서 비교용 `DY`를 계산하는 helper를 분리한다.
    - [x] **T-CCS-3.1.2** `PA` 입력과 합산하여 비교용 `TR`을 계산하는 공통 함수를 작성한다.
    - [x] **T-CCS-3.1.3** 기준 포트폴리오 누락 시 명시적 에러를 반환하는 검증 계층을 구현한다.
- **T-CCS-3.2: 개인운용 시나리오 계산기**
    - [x] **T-CCS-3.2.1** 개인 투자자산 총액과 기준 `TR`로 연간 총수익 계산기를 구현한다.
    - [x] **T-CCS-3.2.2** 개인운용 지역건강보험료 계산을 가구 단위 결과로 연결한다.
    - [x] **T-CCS-3.2.3** 개인 세금 항목을 분리 산출하고 최종 순가처분 현금흐름에 반영한다.
    - [x] **T-CCS-3.2.4** 연간/누적 `세후 순증가액` 계산을 추가한다.
- **T-CCS-3.3: 법인운용 시나리오 계산기**
    - [x] **T-CCS-3.3.1** 동일 기준 `TR`로 법인 총수익 계산기를 구현한다.
    - [x] **T-CCS-3.3.2** 급여 수령자 최대 4명 구조와 직장건보/4대보험 계산을 연결한다.
    - [x] **T-CCS-3.3.3** 회사 부담분과 개인 부담분을 모두 총가구 비용에 포함하는 합산 규칙을 구현한다.
    - [x] **T-CCS-3.3.4** 법인세, 고정 운영비, 급여 총액, 주주대여금 상환을 결합한 순현금 계산기를 구현한다.
    - [x] **T-CCS-3.3.5** 법인 유보이익과 가구 실수령을 분리한 `세후 순증가액` 계산을 구현한다.
- **T-CCS-3.4: 기간 누적 계산**
    - [x] **T-CCS-3.4.1** 사용자 입력 기간을 반영한 누적 총비용/건보료/순자산 계산 루프를 구현한다.
    - [x] **T-CCS-3.4.2** 연도별 결과 시계열을 생성하여 차트 데이터로 반환한다.
- **T-CCS-3.5: 차이 분석 엔진**
    - [x] **T-CCS-3.5.1** 두 시나리오 간 KPI 차이를 자동 계산한다.
    - [x] **T-CCS-3.5.2** `건보료`, `세금`, `급여/4대보험`, `운영비`, `주주대여금 상환`, `유보이익` 기준의 항목별 분해 결과를 생성한다.
    - [x] **T-CCS-3.5.3** 상위 차이 요인 3개를 요약하는 explanation payload를 생성한다.

### [Phase 4] 백엔드 API 및 영속성 통합 (T-CCS-4) - DONE
- **T-CCS-4.1: 설정 API**
    - [x] **T-CCS-4.1.1** 비교 시뮬레이터 설정 조회 API를 추가한다.
    - [x] **T-CCS-4.1.2** 비교 시뮬레이터 설정 저장 API를 추가한다.
    - [x] **T-CCS-4.1.3** 저장 후 재로드 시 값이 안정적으로 유지되도록 파일 영속성을 연결한다.
- **T-CCS-4.2: 실행 API**
    - [x] **T-CCS-4.2.1** 비교 시뮬레이션 실행 API를 추가한다.
    - [x] **T-CCS-4.2.2** 응답에 KPI, 비용 breakdown, assumptions, 차트 데이터, warning 배지를 포함한다.
- **T-CCS-4.3: 기준 연도/정책 메타 정보**
    - [x] **T-CCS-4.3.1** 건강보험 및 세율 계산 기준 연도를 API 응답에 포함한다.
    - [x] **T-CCS-4.3.2** 정책 수치 출처 메타 정보를 응답 또는 별도 엔드포인트로 제공한다.

### [Phase 5] 프런트엔드 탭 및 입력 UI 구현 (T-CCS-5) - DONE
- **T-CCS-5.1: 새 탭 골격 추가**
    - [x] **T-CCS-5.1.1** 전역 네비게이션에 비교 시뮬레이터 전용 탭을 추가한다.
    - [x] **T-CCS-5.1.2** 라우팅/탭 상태와 데이터 로딩 진입점을 연결한다.
- **T-CCS-5.2: 입력 화면 구성**
    - [x] **T-CCS-5.2.1** `가구 정보` 섹션 UI를 구현한다.
    - [x] **T-CCS-5.2.2** `개인 자산`과 `부동산` 섹션 UI를 구현한다.
    - [x] **T-CCS-5.2.3** `기준 포트폴리오 / DY / PA / TR` 표시 및 입력 UI를 구현한다.
    - [x] **T-CCS-5.2.4** `법인 설정` 섹션에서 급여 수령자 최대 4명 관리 UI를 구현한다.
    - [x] **T-CCS-5.2.5** `시뮬레이션 기간` 입력 UI를 구현한다.
- **T-CCS-5.3: 저장/복원 UX**
    - [x] **T-CCS-5.3.1** 저장 버튼, 저장 상태, 재로드 후 복원 UX를 구현한다.
    - [x] **T-CCS-5.3.2** 더미값 없이 빈 초기 상태 또는 명시적 기본값만 노출되도록 정리한다.
- **T-CCS-5.4: 검증 및 에러 UX**
    - [x] **T-CCS-5.4.1** 필수값 누락, 범위 오류, 기준 포트폴리오 누락 상태를 인라인 에러로 표시한다.
    - [x] **T-CCS-5.4.2** 계산 불가 시 결과 그래프 대신 원인과 해결 방법을 노출한다.

### [Phase 6] 비교 결과 UI 및 시각화 구현 (T-CCS-6) - DONE
- **T-CCS-6.1: 상단 KPI 비교 카드**
    - [x] **T-CCS-6.1.1** `개인운용`과 `법인운용` 비교 카드를 나란히 구현한다.
    - [x] **T-CCS-6.1.2** 카드 내 `월 실수령액`, `연 총비용`, `건보료`, `세후 순증가액`을 표시한다.
- **T-CCS-6.2: 비용 분해 차트**
    - [x] **T-CCS-6.2.1** 비용 항목별 stacked bar 또는 동등한 비교 차트를 구현한다.
    - [x] **T-CCS-6.2.2** 툴팁에 항목 설명과 수치를 함께 노출한다.
- **T-CCS-6.3: 순현금흐름 워터폴**
    - [x] **T-CCS-6.3.1** 총수익에서 비용 항목이 차감되는 구조를 보여주는 waterfall 시각화를 구현한다.
- **T-CCS-6.4: 기간 누적 그래프**
    - [x] **T-CCS-6.4.1** 기간별 누적 순자산 또는 누적 실수령 차이 그래프를 구현한다.
- **T-CCS-6.5: 설명 가능한 결과 패널**
    - [x] **T-CCS-6.5.1** 어느 시나리오가 유리한지 한 줄 요약을 구현한다.
    - [x] **T-CCS-6.5.2** 상위 차이 유발 요인 3개를 설명하는 breakdown 패널을 구현한다.
    - [x] **T-CCS-6.5.3** `기준 포트폴리오`, `DY`, `PA`, `TR`, `시뮬레이션 기간`, `기준 연도`를 상시 노출한다.

### [Phase 7] 테스트 및 회귀 고정 (T-CCS-7) - DONE
- **T-CCS-7.1: 백엔드 단위 테스트**
    - [x] **T-CCS-7.1.1** 동일 포트폴리오/동일 TR이 양 시나리오에 공통 적용되는지 테스트한다.
    - [x] **T-CCS-7.1.2** 지역건보 vs 직장건보 차이가 기대대로 계산되는지 테스트한다.
    - [x] **T-CCS-7.1.3** 급여 수령자 수 변경에 따라 총비용이 변하는지 테스트한다.
    - [x] **T-CCS-7.1.4** 주주대여금 상환이 실수령과 잔액에 미치는 영향을 테스트한다.
    - [x] **T-CCS-7.1.5** 부동산 지분율 변경이 개인 시나리오 결과에 반영되는지 테스트한다.
- **T-CCS-7.2: API 통합 테스트**
    - [x] **T-CCS-7.2.1** 설정 저장/재로드/실행 API의 정합성을 검증한다.
    - [x] **T-CCS-7.2.2** 기준 포트폴리오 누락 시 명시적 오류 응답을 검증한다.
- **T-CCS-7.3: Playwright E2E**
    - [x] **T-CCS-7.3.1** 입력 저장 후 재로드해 값이 유지되는지 검증한다.
    - [x] **T-CCS-7.3.2** 급여 수령자 추가/삭제와 결과 카드 갱신을 검증한다.
    - [x] **T-CCS-7.3.3** 기간 변경 시 누적 그래프와 KPI가 함께 갱신되는지 검증한다.
    - [x] **T-CCS-7.3.4** 사용자 실데이터가 테스트로 오염되지 않도록 snapshot/restore를 적용한다.

### [Phase 8] 문서/추적/품질 게이트 정리 (T-CCS-8) - DONE
- **T-CCS-8.1: 문서 정합성**
    - [x] **T-CCS-8.1.1** 본 `plan.md`와 `requirement.md`의 식별자 정합성을 맞춘다.
    - [x] **T-CCS-8.1.2** 필요 시 `test.md`와 `trace_matrix.md`에 항목을 추가한다.
- **T-CCS-8.2: 품질 게이트**
    - [x] **T-CCS-8.2.1** Python 변경분에 대해 `ruff + black --check + 관련 pytest`를 통과시킨다.
    - [x] **T-CCS-8.2.2** Frontend 변경분에 대해 `lint + build + prettier --check + 관련 Playwright`를 통과시킨다.

### [Phase 9] 자산 기반 비교(Asset-driven) 및 상세 감사(Audit) 고도화 (T-CCS-9) - DONE
- **T-CCS-9.1: 백엔드 엔진 순방향 계산 및 감사 데이터 보강**
    - [x] **T-CCS-9.1.1** 보유 자산 총액 기반 세후 현금흐름 순방향 계산기(`Forward CCS`) 구현.
    - [x] **T-CCS-9.1.2** 건보료 점수 산출 중간 과정(재산/소득 등)을 상세히 반환하도록 엔진 수정.
    - [x] **T-CCS-9.1.3** 30년 장기 프로젝션 및 복리 효과 반영 로직 추가.
- **T-CCS-9.2: API 및 데이터 계약 업데이트**
    - [x] **T-CCS-9.2.1** 비교 실행 API 응답에 `audit_details` 및 장기 시계열 데이터 추가.
    - [x] **T-CCS-9.2.2** 시뮬레이션 모드(Target vs Asset) 필드 추가 및 처리.
- **T-CCS-9.3: 프런트엔드 UI/UX 고도화**
    - [x] **T-CCS-9.3.1** 상단 설정 영역에 '비교 모드 스위처' 토글 UI 추가.
    - [x] **T-CCS-9.3.2** KPI 카드 하단에 '상세 비용 감사(Audit) 테이블' 구현.
    - [x] **T-CCS-9.3.3** 30년 장기 자산 추이 시각화 컴포넌트 최적화.
    - [x] **T-CCS-9.3.5** 승자 요약 영역에서 연간/누적 기준 우열과 차액을 즉시 읽히도록 명시적 비교 문구 및 강조 수치 추가.
    - [x] **T-CCS-9.3.6** 우열 판정 로직을 `연 순현금흐름 / 누적 순현금흐름` 기준으로 재정의하고, `주주대여금 상환`을 driver에서 제외한다.
    - [x] **T-CCS-9.3.7** `목표 수익 기반`과 `보유 자산 기반` 모두에서 법인 메인 비교값을 `법인 순현금 + 순급여`로 통일하고, 주주대여금 상환 UI를 메인 비교 축에서 제거한다.
    - [x] **T-CCS-9.3.8** 기본 진입 모드를 `보유 자산 기반`으로 전환하고, 입력/결과 영역 분리 및 최소 11px 타이포 규칙을 비용 비교 화면에 적용한다.
    - [x] **T-CCS-9.3.9** 2026년 법인세율 선택값(10/20/22/25%)을 Settings 및 비용 비교 화면에 연결하고, 지방소득세 포함 실효세율(11/22/24.2/27.5%)로 계산한다.

### [Phase 10] 저장된 Master Portfolio 기반 비용 비교 실행 (T-CCS-10) [NEW]
- **T-CCS-10.1: [Docs] 요구사항/테스트/추적 기준 수립**
    - [x] **T-CCS-10.1.1** 비용 비교가 저장된 `master_portfolio_id`를 기준으로 실행되어야 한다는 요구사항을 `REQ-CCS-25`로 명시한다.
    - [x] **T-CCS-10.1.2** 선택/저장/실행/깨진 참조 차단 테스트 기준을 `TEST-CCS-101`로 정의하고 `trace_matrix`에 등록한다.
- **T-CCS-10.2: [Backend] Master Portfolio 선택값 저장 및 실행 주입**
    - [ ] **T-CCS-10.2.1** `cost_comparison_config.json`에 비용 비교 전용 `master_portfolio_id` 필드를 추가하고 하위 호환 기본값을 정의한다.
    - [ ] **T-CCS-10.2.2** 비교 실행 API가 저장된 `master_portfolio_id`를 우선 사용하고, 미설정 시 활성 master portfolio로 fallback하도록 한다.
    - [ ] **T-CCS-10.2.3** 선택된 master portfolio 또는 내부 참조가 깨진 경우 명시적 오류를 반환하고 임의 수익률 fallback을 금지한다.
- **T-CCS-10.3: [Frontend] 기준 Master Portfolio 선택 UI**
    - [ ] **T-CCS-10.3.1** 비용 비교 입력 영역에 저장된 master portfolio 목록과 현재 선택 전략을 표시한다.
    - [ ] **T-CCS-10.3.2** 선택 변경 시 `master_portfolio_id`를 비용 비교 설정에 저장하고, 실행 결과 assumptions 영역에 실제 사용 전략명을 표시한다.
- **T-CCS-10.4: [Validation] Master Portfolio 기반 실행 정합성 검증**
    - [ ] **T-CCS-10.4.1** 서로 다른 DY를 가진 두 master portfolio를 준비하고 선택값에 따라 비교 결과의 `DY/TR/총수익`이 바뀌는지 API 테스트로 검증한다.
    - [ ] **T-CCS-10.4.2** Playwright로 master portfolio 선택, 저장, 재로드, 실행 결과 assumptions 표시를 검증한다.

### [Phase 11] 법인 운영비 입력 분리 및 세무 비용 반영 (T-CCS-11) [NEW]
- **T-CCS-11.1: [Docs] 운영비 입력 모델 분리 기준 정의**
    - [ ] **T-CCS-11.1.1** 기존 단일 `고정비`를 `월 기장비`와 `연 법인세 조정료`로 분리한다는 요구사항을 `REQ-CCS-45`에 반영한다.
    - [ ] **T-CCS-11.1.2** 운영비 연간 합계가 법인 과세표준과 연 총비용에 반영되어야 한다는 테스트 기준을 `TEST-CCS-102`로 정의하고 `trace_matrix`에 등록한다.
- **T-CCS-11.2: [Backend] 저장 스키마 및 법인 비용 계산 수정**
    - [ ] **T-CCS-11.2.1** 비용 비교 설정의 `corporate` 섹션에 `monthly_bookkeeping_fee`, `annual_corp_tax_adjustment_fee`를 추가하고, 기존 `monthly_fixed_cost` 저장값을 하위 호환으로 흡수한다.
    - [ ] **T-CCS-11.2.2** 법인운용 Target-driven/Asset-driven 계산에서 운영비 연간 합계를 `월 기장비 x 12 + 연 법인세 조정료`로 반영한다.
    - [ ] **T-CCS-11.2.3** 결과 breakdown/audit에 운영비 구성 요소를 노출해 사용자가 연간 운영비 계산 근거를 확인할 수 있게 한다.
- **T-CCS-11.3: [Frontend] 법인 운영비 입력 UI 변경**
    - [ ] **T-CCS-11.3.1** 비용 비교 법인 설정에서 `월 기장비`, `연 법인세 조정료` 입력 필드를 추가하고 기존 단일 `월 고정비` 입력을 제거한다.
    - [ ] **T-CCS-11.3.2** Settings의 법인 운영비 입력도 동일 개념으로 정리하고, 기존 저장값은 사용자에게 자연스럽게 마이그레이션되도록 한다.
- **T-CCS-11.4: [Validation] 회귀 및 저장 정합성 검증**
    - [ ] **T-CCS-11.4.1** 저장/재로드 시 두 운영비 필드가 유지되고, 실행 결과의 연 운영비와 법인세 과세표준이 기대값과 일치하는지 API 테스트로 검증한다.
    - [ ] **T-CCS-11.4.2** Playwright로 두 입력 필드 저장, 재로드, 실행 결과 반영을 검증한다.

## [Execution Order] 구현 착수 순서
1. **백엔드: 순방향 계산 및 감사 데이터 추출 로직 (TDD)**
2. **API: 응답 스키마 확장 및 모드 연동**
3. **프런트엔드: 모드 스위처 및 감사 테이블 UI**
4. **검증: 1년치 정밀 감사 수치 및 30년 프로젝션 정합성**

## [Open Decisions] 구현 전 확인이 필요한 항목
- **D-CCS-01:** 지역건보 계산에서 `금융소득`의 단순 입력값을 받을지, 기준 포트폴리오 `DY` 기반 추정치를 기본으로 둘지 결정 필요.
- **D-CCS-02:** 개인 세금 계산에서 실현/미실현 이익 구분을 1차부터 반영할지, 단순 총수익 기반 추정치로 시작할지 결정 필요.
- **D-CCS-03:** 보수 외 소득월액보험료를 1차에서 정식 계산할지, 경고 배지 수준으로 둘지 결정 필요.
- **D-CCS-04:** 누적 시뮬레이션에서 매년 동일 입력 반복 모델로 갈지, 자산 증가분 재투입(compounding)을 반영할지 결정 필요.

---
*마지막 업데이트: 2026-05-01 17:50:00*
